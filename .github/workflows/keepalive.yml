name: Keep Alive

on:
  schedule:
    - cron: '*/5 * * * *'   # 5분마다 실행

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping sendoff
        run: curl -s https://sendoff-wxg7.onrender.com > /dev/null
